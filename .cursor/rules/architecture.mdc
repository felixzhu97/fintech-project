---
description: Core FinPulse constraints — Clean Architecture layers, monorepo apps, REST, test naming. Deep guides live in skills.
alwaysApply: true
---

# Architecture & FinPulse Core

## Product

**FinPulse** — financial analytics and portfolio platform (admin, portal, mobile) with Go gateway + FastAPI services.

## Dependency Rule

```
handler/web → application → domain ← infrastructure
```

**Domain has NO dependencies on other layers.**

## Terminology Index (Hexagonal ↔ This Project)

| Hexagonal concept | Hexagonal layout | This project layer | This project package (Go) |
|-------------------|------------------|--------------------|---------------------------|
| Driving adapter | `adapter/in` | Handler + Application | `internal/handler/`, `internal/application/` |
| Driven adapter | `adapter/out` | Infrastructure | `internal/infrastructure/` |
| Outbound port | `domain/port` | Repository interface | `internal/application` ports / domain repos |
| Domain core | `domain` | Domain | `internal/domain/` |
| Composition / wiring | `config` | Config / main | `internal/config/`, `cmd/` |

## Forbidden in New Code

- `domain/port/` packages — prefer repository interfaces in application ports or `domain`
- `*Port` interface suffix — prefer `*Repository`, `*Gateway`, or a domain-specific name
- `adapter/in` / `adapter/out` packages — use `handler/`, `application/`, `infrastructure/`

## Layers

| Layer | Contains |
|-------|----------|
| `domain/` | Entities, Value Objects, domain rules |
| `application/` | Use cases, services, repository ports |
| `infrastructure/` | Persistence (pgx), cache, crypto, storage |
| `handler/` / web | HTTP handlers, DTOs |

## Project Structure

```
finpulse/
├── apps/
│   ├── admin/           # React + Vite (management)
│   ├── portal/          # React portal
│   ├── mobile/          # React Native + Expo
│   ├── server-go/       # Go API gateway + CRUD
│   │   └── internal/
│   │       ├── domain/
│   │       ├── application/
│   │       ├── infrastructure/
│   │       ├── handler/
│   │       └── config/
│   └── server-python/   # FastAPI (analytics, quotes, AI/ML)
└── packages/
    ├── analytics/       # @fintech/analytics
    ├── ui/              # @fintech/ui
    └── utils/           # @fintech/utils
```

## Domain Rules

- No framework annotations / driver types in domain
- Entities encapsulate behavior where rules belong
- Repository: interface toward application/domain, implementation in infrastructure
- Prefer glossary Preferred Terms for public names

## Naming (by stack)

| Stack | Type | Rule | Example |
|-------|------|------|---------|
| Go | Domain struct | PascalCase | `Account`, `Portfolio` |
| Go | Repo interface | PascalCase + Repo/Repository | `AccountRepo` |
| Go | Methods | PascalCase (exported) | `GetByID` |
| TS/React | Components | PascalCase | `WatchlistPanel` |
| TS | hooks / vars | camelCase | `useQuotes` |
| REST paths | kebab / resource | plural nouns | `/api/v1/accounts` |

## REST

| Operation | Status |
|-----------|--------|
| Create | 201 |
| Success | 200 |
| No Content | 204 |
| Error | 4xx/5xx |

## Test Naming

Natural language with spaces (no snake_case underscores):

```
should expected result when condition
```

- Vitest / Jest display names: `should refresh quote when symbol is on watchlist`
- Go test names: `TestAccount_GetByID_ReturnsNotFoundWhenMissing`

## Checklist

- [ ] Domain has no outward dependencies
- [ ] No circular dependencies
- [ ] Entities encapsulate behavior
- [ ] UI stays Emotion + existing `@fintech/*` patterns

## Hard constraints (delivery)

When **creating a Jira ticket**, **branching**, **committing**, or **opening a PR**: always follow [developer](.cursor/skills/developer/SKILL.md) §5 (`<type>/<slug>`, Chain PRs, commit/PR templates) and [Product Owner](.cursor/skills/product-owner/SKILL.md). **References** must prefer official documentation and research links ([dependency-docs](.cursor/skills/developer/references/dependency-docs.md), [sources](.cursor/skills/market-tech-analysis/references/sources.md), arXiv).

## Skills (on demand)

| Task | Skill |
|------|-------|
| Feature / tests / commit / Apple UX | [developer](.cursor/skills/developer/SKILL.md) |
| Business Analysis (domain / ubiquitous language / bridge) | [business-analysis](.cursor/skills/business-analysis/SKILL.md) (required for `business-analyst`) |
| Market + tech strategy (moves / competitors / GTM) | [market-tech-analysis](.cursor/skills/market-tech-analysis/SKILL.md) (on demand for `market-analyst` / `tech-analyst`) |
| Product Owner / Jira | [Product Owner](.cursor/skills/product-owner/SKILL.md) |

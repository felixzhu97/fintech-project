# FinPulse C4 架构图

本目录包含 FinPulse（平台后端 + 移动应用）的 C4 架构图。

## 图表列表

| 层级 | 文件 | 描述 |
|------|------|------|
| 容器（平台） | `c4-finpulse-platform-containers.puml` | 后端容器：Go 网关、Python 分析、Java 用户偏好 |
| 上下文 | `c4-finpulse-mobile-context.puml` | 系统级视图，展示移动应用与外部系统的交互 |
| 容器 | `c4-finpulse-mobile-containers.puml` | 移动应用的高层容器架构 |
| 组件 | `c4-finpulse-mobile-components.puml` | 详细的组件架构，包含 hooks、store 和 lib 层 |

说明：若 CI 未渲染 PlantUML，新平台图的 `png/` 为 pending。

## 查看图表

这些 PlantUML 图表可通过以下方式查看：

1. **PlantUML 服务器**: 上传 `.puml` 文件到 [plantuml.com/plantuml](https://www.plantuml.com/plantuml)
2. **IDE 插件**:
   - VS Code: PlantUML 扩展
   - IntelliJ: PlantUML Integration
3. **命令行**: `plantuml diagram.puml`

## 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                         用户                                 │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                    FinPulse 移动应用                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐  │
│  │  页面    │  │  Store   │  │  Hooks    │  │  组件      │  │
│  │ (Expo)   │  │ (Redux)  │  │          │  │            │  │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘  │
│         │            │            │               │         │
│  ┌──────▼────────────▼────────────▼───────────────▼───────┐ │
│  │                      lib / 基础设施层                    │ │
│  │  API 客户端 │ WebSocket │ Web3 │ 网络层               │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────┬───────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
┌───────────────┐ ┌───────────────┐ ┌─────────────────┐
│ 投资组合 API   │ │ 行情 WebSocket│ │ 以太坊 Sepolia  │
└───────────────┘ └───────────────┘ └─────────────────┘
```

## 各层说明

### 应用层 (`app/`)
- **页面**: Expo Router 认证和主标签页
- **布局**: 导航结构的共享布局组件

### 组件层 (`components/`)
- **自选股**: 股票自选管理组件
- **投资组合**: 投资组合可视化组件
- **账户**: 账户管理和交易组件
- **洞察**: 风险指标和分析组件
- **区块链**: 以太坊钱包集成组件
- **原生**: 高性能原生图表模块
- **UI**: 共享 UI 原语 (MetricCard, SortMenu 等)

### Hooks 层 (`hooks/`)
- **认证**: 认证和令牌管理 hooks
- **投资组合**: 投资组合数据获取 hooks
- **市场**: 市场数据和行情流 hooks
- **风险**: 风险指标和 VaR 计算 hooks
- **账户**: 账户数据和偏好设置 hooks
- **区块链**: Web3 和钱包 hooks

### Store 层 (`store/`)
- **authSlice**: 用户认证状态
- **quotesSlice**: 实时股票行情
- **portfolioSlice**: 投资组合数据缓存
- **preferencesSlice**: 用户偏好设置
- **web3Slice**: 以太坊钱包状态

### Lib 层 (`lib/`)
- **api/**: 所有后端服务的 REST API 客户端
- **services/**: QuoteStreamService, Web3Service
- **network/**: HTTP 客户端, 认证桥接, 配置

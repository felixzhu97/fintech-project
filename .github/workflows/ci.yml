name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm --filter './packages/**' build

      - name: Type check
        run: pnpm test:type

      - name: Lint
        run: pnpm lint

  java-bazel:
    name: Java (Bazel)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - uses: bazel-contrib/setup-bazel@0.19.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}
          repository-cache: true

      - name: Test Java controllers
        run: >
          bazel test
          //:AuthControllerTest
          //:CustomerAccountControllerTest
          //:InstrumentControllerTest
          //:WatchlistControllerTest
          //:PortfolioControllerTest
          //:UserPreferenceControllerTest

      - name: Build Java server
        run: bazel build //:server
